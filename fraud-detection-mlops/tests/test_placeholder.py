"""Placeholder test to confirm the environment is wired up correctly.

Replace/expand this as real modules land in src/. Every new module in src/
should get a corresponding test file mirroring its path, e.g.:
src/features/definitions.py -> tests/features/test_definitions.py
"""


def test_environment_sanity() -> None:
    """A trivial test that should always pass once pytest is configured."""
    assert 1 + 1 == 2
